import { useRouter } from 'expo-router';
import { useCallback } from 'react';

export default function navigate() {
  const router = useRouter();

  // const navigate = useCallback((to: Parameters<typeof router.push>) => {
  //   router.push('/')
  // }, [router]);
}
